

type User {
  id: String!
  username: String!
  email: String!
}

type Query {
  users: [User!]!
  findById(ID: String!): User!
}

input NewUser {
  username: String!
  email: String!
}

type Mutation {
  createUser(input: NewUser!): User!
}